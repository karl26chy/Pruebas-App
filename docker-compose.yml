version: '3.8'

services:
  api:
    build:
      context: ./api
    ports:
      - "5000:5000"
    restart: unless-stopped

  client:
    build:
      context: ./client
    ports:
      - "3000:80"
    restart: unless-stopped
    depends_on:
      - api
